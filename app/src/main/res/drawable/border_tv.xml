<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Màu nền -->
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Màu và độ dày của viền -->
    <corners android:radius="8dp" /> <!-- Bo góc -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" /> <!-- Khoảng cách giữa nội dung và viền -->
</shape>
